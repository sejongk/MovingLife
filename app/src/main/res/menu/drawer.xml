<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/setAlarm"
            android:checked="true"
            android:icon="@drawable/ic_place"
            android:title="@string/setAlarm" />
        <item
            android:id="@+id/search"
            android:icon="@drawable/ic_place"
            android:title="@string/search" />
        <item
            android:id="@+id/nearBusStation"
            android:icon="@drawable/ic_place"
            android:title="@string/nearBusStation" />
        <item
            android:id="@+id/busTest"
            android:icon="@drawable/ic_place"
            android:title="@string/busTest" />
    </group>

    <item android:title="@string/setStartEnd">
        <menu>
            <item
                android:icon="@drawable/ic_emoticon"
                android:id="@+id/setStart"
                android:title="@string/setStart" />
            <item
                android:icon="@drawable/ic_emoticon"
                android:id="@+id/setEnd"
                android:title="@string/setEnd" />
        </menu>
    </item>
</menu>
